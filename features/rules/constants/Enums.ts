export enum WinningRequirement {
    WinByTwo = "Win By Two",
    FirstToGamePoint = "First To Game Point"
};

export enum PointsBy {
    PointPerRally = "Point Per Rally",
    PointOnServe = "Point On Serve",
}